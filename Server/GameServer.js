class GameServer {
    constructor () {
        this.clients = [];
    }

    addClient (socket) {
        this.clients.push()
    }
}

const gameServer = new GameServer()

module.exports = gameServer;